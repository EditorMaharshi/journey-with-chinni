// Add this method to PlayerCombat.cs
public void UpdateStatsAfterSkill()
{
    // Re-check max health and update UI if necessary
    // Example: UIManager.Instance.UpdateHealth(currentHealth, maxHealth); 
}
